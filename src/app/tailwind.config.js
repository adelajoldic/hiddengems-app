/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // Scans all your HTML and TypeScript files for Tailwind classes
  ],
  theme: {
    extend: {}, // Customize your theme here if needed
  },
  plugins: [],
};
